#
# Target configuration file for z88dk
#

# Asm file which contains the startup code (without suffix)
CRT0		 DESTDIR/lib/target/sam/classic/sam_crt0

# Any default options you want - these are options to zcc which are fed
# through to compiler, assembler etc as necessary
OPTIONS		 -O2 -SO2 -iquote. -DZ80 -DSAM -D__SAM__ -M -clib=default -Cc-standard-escape-chars

CLIB		default -lsam_clib -lndos
CLIB		ansi  -pragma-need=ansiterminal -D__CONIO_VT100 -lsam_clib -lndos

INCLUDE alias.inc
