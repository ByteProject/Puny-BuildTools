	ld	l,(hl)
	ld	h,0
	push	hl
	ld	hl,%2	;const
	add	hl,sp
	call	l_gchar
	pop	de
	ld	h,e
	mlt	hl
=
	ld	e,(hl)
	ld	hl,%2 - 2	;const
	add	hl,sp
	ld	d,(hl)
	ex	de,hl
	mlt	hl

	call	l_gcharspsp	;
	ld	hl,%1	;const
	add	hl,sp
	ld	l,(hl)
	ld	h,0
	pop	de
	ld	h,e
	mlt	hl
=
	add	hl,sp
	ld	e,(hl)
	ld	hl,+(%1 - 2)	;const
	add	hl,sp
	ld	d,(hl)
	ex	de,hl
	mlt	hl

	push	hl
	ld	hl,%1	;const
	add	hl,sp
	call	l_gchar
	pop	de
	ld	h,e
	mlt	hl
=
	ld	e,l
	ld	hl,+(%1 - 2)	;const
	add	hl,sp
	ld	d,(hl)
	ex	de,hl
	mlt	hl

	ld	l,h
	ld	h,0
	ld	e,l
	ld	hl,%1
=
	ld	e,h
	ld	hl,%1
