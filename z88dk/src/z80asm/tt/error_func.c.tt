/* generated by Template::Toolkit - do not edit */
[% TAGS outline -%]
%% USE YAML; data = YAML.undumpfile('error_func.yaml')

#include "errors.h"
#include "error_func.h"
#include "str.h"
#include <stdio.h>
#include <stdlib.h>

%% FOREACH data.errors
void [% func %]([% args %])
{
	STR_DEFINE(msg, STR_SIZE);

	Str_append_sprintf( msg, [% message %] );
	do_error( [% type %], Str_data(msg) );
	
	STR_DELETE(msg);
}
%% END
